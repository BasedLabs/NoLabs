name: NoLabs build

on:
  push:
    branches: feature/cicd
    #paths:
    #  - 'build/**'
    #  - 'client/**'
    #  - 'frontend/**'
    #  - 'nolabs/**'
    #  - 'build/**'

jobs:
  nolabs-build:
    permissions:
      contents: read
      packages: write

    uses: ./.github/workflows/build-docker.yaml
    with:
      microservice_name: "nolabs"